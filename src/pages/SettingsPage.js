// src/pages/SettingsPage.js
import React from 'react';
import { Box, Typography } from '@mui/material';

const SettingsPage = () => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h6" color="#fff">Cài đặt</Typography>
  </Box>
);

export default SettingsPage;