// src/pages/ProfilePage.js
import React from 'react';
import { Box, Typography } from '@mui/material';

const ProfilePage = () => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h6" color="#fff">Trang cá nhân</Typography>
  </Box>
);

export default ProfilePage;