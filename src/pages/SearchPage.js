// src/pages/SearchPage.js
import React from 'react';
import { Box, Typography } from '@mui/material';

const SearchPage = () => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography variant="h6" color="#fff">Tìm kiếm (Mobile)</Typography>
  </Box>
);

export default SearchPage;